---
- host: all
  gather_info: false
  
  tasks:
  
- name: Deploy SSH Key
     authorized_key: user=devops
                     key="{{ lookup('file', '/root/.ssh/id_rsa.pub') }}"
                     state=present
